const konnichiha = (name:string) => {
    return `Hello, ${name}!`;
}

console.log(konnichiha('Kiyohara Kazuhiro'));


const calcSample = (a:number, b:number): number => {
    return a + b;
}

console.log(calcSample(1,2));